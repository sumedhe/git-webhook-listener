# Description: This file contains the endpoint configurations of the Choreo project.
version: 0.1

# Define the endpoints of the project.
endpoints:
    # Define the GitHub Webhook Receiver endpoint.
  - name: Github Webhook Receiver
    port: 8080
    type: REST
    networkVisibility: Public
    context: /
    schemaFilePath: openapi.yaml
